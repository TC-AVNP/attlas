---
title: "Day 9 — Terminal Mobile Scroll, Petboard Handoff, Birth Skill, Revista Maria Tennis"
date: 2026-05-02
day: 9
duration: "8h"
projects: [terminal, petboard, builder-s-knowledge-base, dotfiles, attlas-file-manager, revista-maria]
sessions:
  - id: "cfb17c22-ed90-43d1-98b0-d064ea587c47"
    gcs: "gs://attlas-session-transcripts/2026/05/cfb17c22-ed90-43d1-98b0-d064ea587c47.jsonl"
    start: "10:03 UTC"
    end: "10:07 UTC"
  - id: "10a79d63-a475-42fd-86a7-90a2f847820b"
    gcs: "gs://attlas-session-transcripts/2026/05/10a79d63-a475-42fd-86a7-90a2f847820b.jsonl"
    start: "09:37 UTC"
    end: "10:08 UTC"
  - id: "fea06b6b-1164-4eef-b589-592ec840de0d"
    gcs: "gs://attlas-session-transcripts/2026/05/fea06b6b-1164-4eef-b589-592ec840de0d.jsonl"
    start: "09:43 UTC"
    end: "11:03 UTC"
  - id: "4732561e-f181-438e-b4c9-2817a094405d"
    gcs: "gs://attlas-session-transcripts/2026/05/4732561e-f181-438e-b4c9-2817a094405d.jsonl"
    start: "11:06 UTC"
    end: "11:16 UTC"
  - id: "8d9a7143-20a2-4f33-a987-e04284f5c22a"
    gcs: "gs://attlas-session-transcripts/2026/05/8d9a7143-20a2-4f33-a987-e04284f5c22a.jsonl"
    start: "11:16 UTC"
    end: "11:27 UTC"
  - id: "f94de05e-0580-49c7-85b9-afdffdc7d554"
    gcs: "gs://attlas-session-transcripts/2026/05/f94de05e-0580-49c7-85b9-afdffdc7d554.jsonl"
    start: "11:26 UTC"
    end: "11:30 UTC"
  - id: "11748c39-7de1-442e-b836-c3283d9b6892"
    gcs: "gs://attlas-session-transcripts/2026/05/11748c39-7de1-442e-b836-c3283d9b6892.jsonl"
    start: "12:24 UTC"
    end: "17:28 UTC"
---

Seven sessions across the day. Morning: mobile scroll fix, petboard handoff, /birth startup skill, knowledge MCP binary, and skill improvements after an AFM build exposed gaps. Afternoon: planned and built Revista Maria Tennis from scratch — a tournament bracket web app for casual tennis sessions with friends. Go server, SQLite, single-elimination brackets with doubles pairs, admin backoffice behind passphrase, mobile-first public bracket view. Deployed to rm.attlas.uk. Multiple live iterations during an actual tennis session — switching from singles to doubles, adding 3rd/4th place match, fixing scores mid-tournament.

## What got done

- **terminal**: Added always-visible scroll button to mobile floating toggle group
- **petboard**: Added action bar with Handoff modal (markdown export with feature selector) and Work on it placeholder
- **terminal**: Auto-run /birth on Claude startup with loading screen in ttyd-tmux.sh
- **dotfiles**: Updated /birth to load KB index, updated /noah with explicit KB entry checklist
- **builder-s-knowledge-base**: Committed knowledge-mcp binary, added mcp-binary-convention entry, backlog feature for graph depth levels
- **revista-maria**: Built and deployed Revista Maria Tennis — Go server with SQLite, doubles bracket, admin backoffice, mobile-first public view at rm.attlas.uk

<!-- detailed -->

## Timeline

### Mobile scroll button fix (~4min, 10:03 – 10:07)

- Identified that `#mk-scroll` was inside `#mk` (the keyboard overlay, hidden by default) while desktop had a separate `#mk-scroll-desktop` in `#mk-desktop-btns`
- Added `#mk-scroll-mobile` button to `#mk-toggles` — the floating button group always visible on touch devices
- Wired it to the existing `_mkToggleScroll()` function with touchstart + click handlers
- Re-ran `install.sh` to rebuild the custom ttyd index with the updated fragment
- Verified the button appears in the deployed HTML

### Planning handoff feature (~5min, 09:37 – 09:42)

- Used /god to plan feature #158: project action bar with Handoff and Work on it buttons
- Ran collision check against existing features — found overlaps with #35 (done, lost in UI rebuild) and #84 (Start building, already shipped)
- Resolved: Handoff restores lost export, Work on it reuses existing terminal launcher
- Refined feature description: handoff defaults to one-click full export, interactive panel only on request

### Building the handoff feature (~25min, 09:42 – 10:08)

- Read codebase: Kanban.tsx, ProjectDetail.tsx, api client, types
- First iteration: two separate buttons (Handoff + Work on it) — user wanted single Actions dropdown
- Second iteration: single Actions dropdown menu — user wanted two buttons, not a dropdown
- Final iteration: two buttons in header, Handoff opens a modal with project description, "Select details" expandable section with feature checkboxes, Download button
- Built markdown generation: project name, problem, description, features grouped by status
- Deployed three times iterating on the UX based on user feedback

### Birth skill and terminal auto-run (~1.5h, 09:43 – 11:16)

- Created `/birth` skill: loader at `birth.md` and full instructions at `birth/SKILL.md`
- Skill calls `knowledge.get_entry(slug: "first-day")` to orient new Claude sessions
- Modified `ttyd-tmux.sh` to auto-send `run /birth` via tmux send-keys after Claude loads
- Added loading screen that waits for "father" greeting or 45s max timeout before attaching
- Crashed the terminal 3 times — first by overcomplicating with hooks, then by killing attached tmux sessions
- Deployed updated script to `/usr/local/bin/ttyd-tmux.sh`
- Committed the `knowledge-mcp` Go binary (was untracked)
- Created knowledge base entry `mcp-binary-convention` linked to `build-mcp-server` and `mcp-servers`

### Revista Maria Tennis — full build (~5h, 12:24 – 17:28)

- Used /god to plan the project — tennis tournament bracket app for casual sessions with friends
- Created petboard project for Revista Maria Tennis
- Built from scratch: Go server on port 7696, SQLite storage, Caddy site block for rm.attlas.uk
- Public view: bracket display, current match, who's next — no auth required
- Admin backoffice: passphrase-protected (rm2026), register players, start tournament, record scores
- First iteration was singles (1v1), first to 6 points — user switched to doubles mid-session
- Added players: Spadz, Condeco, Jfig, Rosinha, PP13, RV, Maria, Gordo
- Randomised doubles pairs, generated single-elimination bracket
- User demanded mobile-first — everything redesigned for phone screens
- Multiple live iterations during the actual tournament: bracket canvas view, zoom in/out, score recording
- Added 3rd/4th place match alongside the final
- User corrected scores and brackets multiple times mid-tournament as real matches were played
- Tournament ran to completion: semi-finals, 3rd place match, final
- Created knowledge base entry on how to send Telegram notifications (prompted by user wanting build notifications)

### AFM build attempt and skill improvement (~15min, 11:16 – 11:30)

- Attempted to build AFM features 1-4 via /noah
- Agent read `create-new-project` KB entry but then went straight to reading raw reference service files (install.sh, main.go, templates) instead of checking KB for design-system, anatomy, port-registry entries
- User caught the pattern: /birth didn't load the KB index, /noah didn't list which entries to read
- Fixed /birth: added Step 2 to call `list_entries()` so the agent always knows what KB articles exist
- Fixed /noah: Step 0.3 now has explicit entry lists — greenfield builds must read `create-new-project`, `anatomy`, `design-system`, `port-registry`, `infrastructure`
- Added feature #159 to knowledge base backlog: structure graph with depth levels so agents can index by depth

## Project summaries

### terminal

Fixed mobile scroll button visibility. The scroll toggle (tmux copy-mode) was only accessible when the custom keyboard was open. Added a dedicated scroll button to the `#mk-toggles` floating group so it's always reachable on mobile. Also wired up `/birth` auto-run in `ttyd-tmux.sh` — new sessions now auto-orient via the knowledge base before the user sees the terminal.
**Lines:** 1-122 (scroll fix), fea06b6b:1-673 (birth auto-run)

### petboard

Added feature #158 — project action bar on the detail page header. Two buttons: "Handoff" opens a modal explaining the download with an expandable feature selector (all pre-ticked), downloads a markdown file with full project context. "Work on it" is a greyed-out placeholder. Involved three iterations to get the UX right based on user feedback.
**Lines:** 3-210

### dotfiles

Created the `/birth` skill — two-file structure with `birth.md` loader and `birth/SKILL.md` instructions. The skill reads the First Day knowledge base entry and internalizes ecosystem context. Later updated /birth to also call `list_entries()` (Step 2) and updated /noah's Step 0.3 with explicit KB entry checklists per scenario.
**Lines:** fea06b6b:200-500, f94de05e:1-173

### builder-s-knowledge-base

Committed the `knowledge-mcp` binary to git (was built but untracked). Added `mcp-binary-convention` knowledge entry documenting that stdio MCP binaries must be committed so they work immediately after clone. Added feature #159 to backlog: graph depth levels for `list_entries(max_depth=N)`.
**Lines:** 4732561e:1-100, f94de05e:80-173

### attlas-file-manager

No code shipped. Attempted build exposed that agents skip KB entries when /noah doesn't explicitly list them. The session was redirected to fixing the skill gap instead.
**Lines:** 8d9a7143:1-173, f94de05e:1-80

### revista-maria

Built Revista Maria Tennis end-to-end in a single session. Go server with SQLite, deployed to rm.attlas.uk behind Caddy. Public bracket view (mobile-first, no auth) and admin backoffice (passphrase-protected). Started as singles first-to-6, pivoted to doubles first-to-3 mid-session based on the actual tennis group composition. Registered 8 players as 4 doubles pairs, generated single-elimination bracket, added 3rd/4th place match. User ran a real tournament during the build — scores were entered live, bugs fixed in real time. Created a knowledge base entry for Telegram notifications along the way.
**Lines:** 11748c39:1-726

## Lessons

- The terminal's mobile UI has two layers: `#mk-toggles` (always visible floating buttons) and `#mk` (the full keyboard overlay, toggled on/off). Features that should always be accessible must go in the toggles, not inside the keyboard.
- Listen more carefully to the user's UI description before building. "A button with two options" means a single trigger with choices inside, not two buttons or a dropdown. Three iterations could have been one.
- When views get deleted (canvas, list view), audit for orphaned features that depended on UI elements in those views. The export from #35 was silently lost when the kanban replaced the list view.
- Never kill tmux sessions marked as `(attached)` — that's the user's live terminal. Filter them out before batch-killing.
- Commit compiled Go binaries for stdio MCP servers so they work immediately after clone without needing a build step.
- A single KB query is never enough. When /noah says "search if you need to," agents optimize for speed and skip it. Explicit checklists ("read these 5 entries") work; vague guidance ("search if needed") doesn't.
- Building a product while it's being used live is intense but produces the best feedback loop — real users catch things mock testing never would.
- Get the project name right from the start. Renaming petboard slugs after creation is a pain — the original wrong name sticks in taxonomy pages and MCP queries.
- Singles-to-doubles is not a minor change — it touches the data model (teams vs players), bracket generation, scoring, and every template. Scope the format before building.
