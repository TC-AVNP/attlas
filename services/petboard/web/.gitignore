node_modules
dist
*.local
.vite

# tsc -b with composite projects (tsconfig.node.json) emits declarations
# and JS for the referenced config files. We don't need any of those
# checked in — they are regenerated on every build.
*.tsbuildinfo
vite.config.js
vite.config.d.ts
